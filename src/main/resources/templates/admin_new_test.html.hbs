{{#> _layout.html.hbs }}

<div class="container">
    <h1>
        {{page_title}}
        {{> _profile.html.hbs }}
    </h1>

    <div>
        <form class="form-horizontal" method="post" action="/admin/tests">
            <div class="form-group">
                <div class="col-sm-12">
                    <input type="text" class="form-control" name="name" rows="3" placeholder="Название" required>
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-2">
                    <button type="submit" class="btn btn-success">Добавить</button>
                </div>
            </div>
        </form>
    </div>
</div>

{{/_layout.html.hbs}}
